/**
 * @todo 
 * 首先假定所有的应用都采用hashhistory路由
 * 并且都采用React-router v4
 */
export default class Spore {
  constructor() {
  }

  registerSingleSPA() {
  }

  start() {
  }

  reroute() {
    /**
     * @desc
     * 当主项目监听到路由发生变化时，会调用reroute函数,
     * 卸载不满足路由条件的应用，并加载正确的应用
     */
  }
};
